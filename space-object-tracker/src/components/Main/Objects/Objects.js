import { ObjectItem } from "./ObjectItem";

export const Objects = () => {
    return (
        <section id="catalog-page">
        <h1>All Objects</h1>

        {/* {games.length > 0
            ? games.map(x => <ObjectItem key={x._id} game={x} />)
            : <h3 className="no-articles">No articles yet</h3>
        } */}
    </section>
    );
}